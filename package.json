{
  "name": "chrome-tab-remote",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "precommit": "npm run typecheck && npm run lint && npm run test && npm audit --omit=dev --audit-level=moderate",
    "package:store": "node scripts/package-store.mjs",
    "verify:store-package": "node scripts/verify-store-package.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "esbuild": "^0.28.1",
    "eslint": "^10.9.1",
    "jsdom": "^30.0.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.11"
  }
}
